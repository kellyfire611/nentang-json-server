{
  "name": "nentang-json-server",
  "version": "1.0.0",
  "description": "NenTang JSON Server",
  "main": "index.js",
  "scripts": {
    "start": "json-server database.json --port 3004 --host 0.0.0.0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "kellyfire",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
